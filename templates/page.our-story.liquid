<div class="our-story">
  <!-- HERO SECTION -->
  <section class="story-hero">
    <div class="story-hero-content">
      <span class="story-eyebrow">OUR STORY</span>
      <h1 class="story-title">To Live Beautifully</h1>
      <p class="story-subtitle">Vivere — from the Italian, meaning "to live." Because every night you wear something beautiful is a night you truly lived.</p>
    </div>
  </section>

  <!-- ORIGIN SECTION -->
  <section class="story-section">
    <div class="story-wrap">
      <div class="story-text">
        <h2>It Started with a Question</h2>
        <p>
          Why does luxury end the moment you close your bedroom door?
        </p>
        <p>
          We noticed something strange about the world of nightwear. The fabrics were either beautiful or affordable — never both. Women who curated every detail of their wardrobe, their skincare, their homes, were settling for ordinary the moment they reached for something to sleep in.
        </p>
        <p>
          <strong>We refused to accept that.</strong> So we set out to create what didn't exist — nightwear that honours the woman wearing it. Pieces so considered, so beautifully made, that slipping into them feels like a quiet act of self-respect.
        </p>
      </div>
      <div class="story-image">
        <img src="{{ 'mn2477or335oq8.jpeg' | asset_url }}" alt="Blush pink satin pajama set laid on an unmade bed — the Vivere morning" loading="lazy">
      </div>
    </div>
  </section>

  <!-- PHILOSOPHY SECTION -->
  <section class="story-section story-section-reverse">
    <div class="story-wrap">
      <div class="story-image">
        <img src="{{ 'mn2432kkjgrobh.jpeg' | asset_url }}" alt="Vivere fabric swatches in ivory, rose, and navy with tailoring tools — our craft" loading="lazy">
      </div>
      <div class="story-text">
        <h2>The Vivere Distinction</h2>
        <p>
          <strong>We don't believe luxury should be gatekept.</strong> The same modal, the same satin, the same attention to seam and silhouette that defines the world's most coveted sleepwear — we bring it to you without the markup.
        </p>
        <p>
          Every piece is built on four principles we will never compromise:
        </p>
        <ul class="story-list">
          <li><strong>Fabric First.</strong> Modal blends, satin finishes, breathable cotton — sourced from the same mills that supply international fashion houses.</li>
          <li><strong>Intentional Design.</strong> Flattering cuts, considered details, tested on real bodies. Nothing accidental.</li>
          <li><strong>Attainable Indulgence.</strong> We kept the quality and stripped the excess. What you pay for is the garment — not the brand mythology.</li>
          <li><strong>Honest Craft.</strong> Responsibly sourced. Ethically manufactured. Built to endure.</li>
        </ul>
      </div>
    </div>
  </section>

  <!-- BELIEF SECTION -->
  <section class="story-belief">
    <div class="belief-content">
      <blockquote class="belief-quote">
        "A woman who sleeps beautifully wakes up differently. That's not vanity — that's self-care in its most intimate form."
      </blockquote>
    </div>
  </section>

  <!-- VALUES SECTION -->
  <section class="story-values">
    <h2>What We Stand For</h2>
    <div class="values-grid">
      <div class="value-card">
        <div class="value-icon">&#10022;</div>
        <h3>Uncompromised Quality</h3>
        <p>Modal that falls like silk. Satin that catches light. Lace that breathes against skin. The fabrics tell the story before we ever do.</p>
      </div>
      <div class="value-card">
        <div class="value-icon">&#9671;</div>
        <h3>Quiet Confidence</h3>
        <p>Vivere isn't loud. It's the feeling of slipping into something exquisite and knowing — without needing anyone to tell you — that you look extraordinary.</p>
      </div>
      <div class="value-card">
        <div class="value-icon">&#9675;</div>
        <h3>Responsible Beauty</h3>
        <p>Our suppliers are paid fairly. Our manufacturers uphold ethical standards. Our fabrics are sourced with conscience. True luxury never comes at someone else's expense.</p>
      </div>
      <div class="value-card">
        <div class="value-icon">&#9653;</div>
        <h3>Designed by You</h3>
        <p>Every collection is shaped by the women who wear it. Your feedback guides our fabric choices, our sizing, our silhouettes. You're not a customer — you're our collaborator.</p>
      </div>
    </div>
  </section>

  <!-- STATS SECTION -->
  <section class="story-stats">
    <div class="stats-intro">
      <p>In numbers</p>
    </div>
    <div class="stats-wrap">
      <div class="stat-item">
        <div class="stat-number">4,200+</div>
        <div class="stat-label">Women Who Chose Vivere</div>
      </div>
      <div class="stat-item">
        <div class="stat-number">4.9&#9733;</div>
        <div class="stat-label">Average Rating</div>
      </div>
      <div class="stat-item">
        <div class="stat-number">12</div>
        <div class="stat-label">Curated Collections</div>
      </div>
      <div class="stat-item">
        <div class="stat-number">98%</div>
        <div class="stat-label">Would Recommend</div>
      </div>
    </div>
  </section>

  <!-- CTA SECTION -->
  <section class="story-cta">
    <h2>Experience the Difference</h2>
    <p>Join the women who decided that every night deserves to feel extraordinary.</p>
    <a href="/collections/all" class="story-btn">Explore the Collection</a>
  </section>
</div>

<style>
.our-story { background: var(--ivory); }

/* HERO */
.story-hero {
  background: var(--midnight);
  color: var(--ivory);
  padding: clamp(100px, 14vh, 180px) var(--gutter);
  text-align: center;
}
.story-eyebrow {
  display: block;
  font-size: 11px;
  letter-spacing: 0.25em;
  text-transform: uppercase;
  margin-bottom: 24px;
  color: var(--caramel);
  font-weight: 500;
}
.story-title {
  font-family: var(--serif);
  font-size: clamp(44px, 7vw, 72px);
  line-height: 1.1;
  margin-bottom: 20px;
  font-weight: 400;
  font-style: italic;
}
.story-subtitle {
  font-size: 17px;
  color: var(--champagne);
  max-width: 540px;
  margin: 0 auto;
  line-height: 1.7;
  font-weight: 300;
}

/* SECTIONS */
.story-section {
  padding: clamp(80px, 10vw, 120px) var(--gutter);
  max-width: var(--content-max);
  margin: 0 auto;
}
.story-wrap {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: clamp(48px, 6vw, 80px);
  align-items: center;
}
.story-section-reverse .story-wrap {
  grid-auto-flow: dense;
}
.story-text h2 {
  font-family: var(--serif);
  font-size: clamp(32px, 4vw, 48px);
  margin-bottom: 28px;
  color: var(--midnight);
  font-weight: 400;
  line-height: 1.15;
}
.story-text p {
  font-size: 16px;
  line-height: 1.85;
  color: var(--text-dark);
  margin-bottom: 20px;
  font-weight: 300;
}
.story-text strong {
  font-weight: 500;
  color: var(--midnight);
}
.story-list {
  list-style: none;
  margin: 28px 0;
  padding: 0;
}
.story-list li {
  font-size: 16px;
  line-height: 1.85;
  color: var(--text-dark);
  margin-bottom: 14px;
  padding-left: 24px;
  position: relative;
  font-weight: 300;
}
.story-list li::before {
  content: "\2014";
  position: absolute;
  left: 0;
  color: var(--caramel);
  font-weight: 400;
}
.story-image img {
  width: 100%;
  height: auto;
  border-radius: 2px;
  box-shadow: var(--shadow-lg);
}

/* BELIEF / QUOTE */
.story-belief {
  background: var(--cream);
  padding: clamp(60px, 8vw, 100px) var(--gutter);
}
.belief-content {
  max-width: 720px;
  margin: 0 auto;
  text-align: center;
}
.belief-quote {
  font-family: var(--serif);
  font-size: clamp(22px, 3vw, 32px);
  font-style: italic;
  font-weight: 400;
  line-height: 1.6;
  color: var(--midnight);
  margin: 0;
  position: relative;
}
.belief-quote::before {
  content: "\201C";
  display: none;
}

/* VALUES */
.story-values {
  padding: clamp(80px, 10vw, 120px) var(--gutter);
}
.story-values h2 {
  font-family: var(--serif);
  font-size: clamp(32px, 4vw, 48px);
  text-align: center;
  margin-bottom: 56px;
  color: var(--midnight);
  font-weight: 400;
}
.values-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 40px;
  max-width: var(--content-max);
  margin: 0 auto;
}
.value-card {
  padding: 36px 28px;
  text-align: center;
  border-top: 1px solid var(--border);
  transition: transform 0.3s var(--ease-out);
}
.value-card:hover {
  transform: translateY(-6px);
}
.value-icon {
  font-size: 28px;
  margin-bottom: 20px;
  display: block;
  color: var(--caramel);
}
.value-card h3 {
  font-family: var(--serif);
  font-size: 20px;
  margin-bottom: 14px;
  color: var(--midnight);
  font-weight: 400;
}
.value-card p {
  font-size: 15px;
  line-height: 1.7;
  color: var(--text-mid);
  font-weight: 300;
}

/* STATS */
.story-stats {
  background: var(--midnight);
  color: var(--ivory);
  padding: clamp(80px, 10vw, 120px) var(--gutter);
}
.stats-intro {
  text-align: center;
  margin-bottom: 56px;
}
.stats-intro p {
  font-family: var(--serif);
  font-size: 18px;
  color: var(--caramel);
  font-weight: 400;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}
.stats-wrap {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 40px;
  max-width: var(--content-max);
  margin: 0 auto;
  text-align: center;
}
.stat-item {
  padding: 24px;
  border-top: 1px solid rgba(201, 169, 110, 0.25);
}
.stat-number {
  font-family: var(--serif);
  font-size: clamp(42px, 6vw, 64px);
  font-weight: 400;
  color: var(--caramel);
  margin-bottom: 12px;
  display: block;
  line-height: 1;
}
.stat-label {
  font-size: 12px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--champagne);
  font-weight: 400;
  line-height: 1.5;
}

/* CTA */
.story-cta {
  padding: clamp(80px, 10vw, 120px) var(--gutter);
  text-align: center;
  max-width: var(--content-max);
  margin: 0 auto;
}
.story-cta h2 {
  font-family: var(--serif);
  font-size: clamp(32px, 4vw, 48px);
  margin-bottom: 16px;
  color: var(--midnight);
  font-weight: 400;
}
.story-cta p {
  font-size: 17px;
  color: var(--text-mid);
  margin-bottom: 36px;
  font-weight: 300;
  line-height: 1.7;
}
.story-btn {
  display: inline-block;
  background: var(--midnight);
  color: var(--ivory);
  padding: 16px 48px;
  font-size: 11px;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  font-weight: 500;
  transition: background 0.3s var(--ease-out);
}
.story-btn:hover {
  background: var(--rose);
}

/* RESPONSIVE */
@media (max-width: 768px) {
  .story-wrap {
    grid-template-columns: 1fr;
    gap: 32px;
  }
  .story-section-reverse .story-wrap {
    grid-auto-flow: row;
  }
  .story-text h2 {
    font-size: 28px;
  }
  .story-title {
    font-size: 36px;
  }
  .belief-quote {
    font-size: 20px;
  }
  .values-grid {
    grid-template-columns: 1fr;
    gap: 24px;
  }
  .stats-wrap {
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
  }
}
</style>
